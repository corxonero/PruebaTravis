language: php
php:
  - 5.6
sudo: required
dist: trusty
before_install: echo 'before install'
before_script:
  - echo 'Antes de script'
  - pwd
  - ls -a
  - git status
script: phpunit Test.php
after_success:
  - echo 'after success'
    - "curl -o /tmp/travis-automerge https://raw.githubusercontent.com/cdown/travis-automerge/master/travis-automerge"
  - "chmod a+x /tmp/travis-automerge"
  - "BRANCHES_TO_MERGE_REGEX='^f/' BRANCH_TO_MERGE_INTO=master GITHUB_REPO=https://github.com/AgoraUS-G1-1617/Autentication /tmp/travis-automerge"
after_script: echo 'after scrip'
